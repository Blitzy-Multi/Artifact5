{
  "name": "artifact5-hello-service",
  "version": "1.0.0",
  "private": true,
  "description": "Node.js tutorial project serving a single HTTP endpoint, GET /hello, which returns Hello world to the calling HTTP client.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:watch": "node --test --watch"
  },
  "engines": {
    "node": "^24.0.0"
  },
  "dependencies": {
    "express": "5.2.1"
  },
  "devDependencies": {
    "supertest": "7.2.2"
  }
}
